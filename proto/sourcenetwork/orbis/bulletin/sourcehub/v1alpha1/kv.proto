syntax = "proto3";

package orbis.bulletin.sourcehub.v1alpha1;

// Pairs defines a repeated slice of Pair objects.
message Pairs {
    repeated Pair pairs = 1;
}
  
// Pair defines a key/value bytes tuple.
message Pair {
    bytes key   = 1;
    bytes value = 2;
}